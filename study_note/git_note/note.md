* 기본설정
```
git init //git 폴더 생성

git remote add origin (URL) // 깃과 깃허브 연결

git add //임시저장

git commit -m '멘트' //저장확정

git push origin master //업로드 완료
```
* 깃 업로드
```
git config --global user.name ''
git config --global user.email '이메일'
git config --global core editor nano

```